apiVersion: apps/v1
kind: Deployment
metadata:
  name: litellm
  namespace: veto-prod
spec:
  template:
    spec:
      containers:
        - name: litellm
          resources:
            requests:
              memory: "512Mi"
            limits:
              memory: "1Gi"
